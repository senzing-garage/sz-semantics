[project]

name = "sz_semantics"
version = "0.1.0"
description = "Transform JSONL output from Senzing SDK for use with graph technologies, semantics, and LLM integration"

authors = [
    { name = "Paco Nathan", email = "paco@senzing.com" }
]

license = { text = "MIT" }
readme = "README.md"

requires-python = ">=3.11"

dependencies = [
]


[tool.poetry]

requires-poetry = ">=2.1"


[tool.poetry.group.dev.dependencies]

pytest = "^8.4.2"


[build-system]

requires = [
    "poetry-core>=2.0.0,<3.0.0",
]

build-backend = "poetry.core.masonry.api"
